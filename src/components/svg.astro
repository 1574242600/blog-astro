---
import { Icon } from 'astro-icon/components'

interface Props {
    id: string
    class?: string
}

const { id, class: _class = '' } = Astro.props
---

<Icon 
    name={`mdi:${id}`}
    class={`align-middle ${_class}`}
></Icon>