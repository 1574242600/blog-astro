---
import PostHeader from './postHeader.astro'
import Divider from './divider.astro'
import Box from './box.astro'

import { type PostFrontmatter } from '@service/postService'

interface Props {
    frontmatter: PostFrontmatter
    path: string
}

const widthClass = ' xl:w-3xl lg:w-2xl md:w-xl w-full'
const buttonClass =
    'inline-block w-20 h-8 p-2 ' +
    'text-sm font-bold font-mono ' +
    'text-base-200 bg-secondary-content hover:bg-secondary ' +
    'shadow-sm'

const { frontmatter, path } = Astro.props
const { excerpt } = frontmatter
---

<Box className={'p-6 ' + widthClass}>
    <article>
        <PostHeader frontmatter={frontmatter} titleLevel={2} />
        <Divider />
        <div class='heti m-auto pt-4 max-w-2xl!' set:html={excerpt.html} />
    </article>
    <a class='block text-center mt-4' href={path}>
        <div class={buttonClass}>阅读更多</div>
    </a>
</Box>
