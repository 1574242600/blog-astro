---
interface Props {
    href: string
    label?: string
}

const { href, label } = Astro.props;
---

<a class='hover:text-blue-500' aria-label={label ? label : 'A link'}  href={href} target='_blank' rel='noreferrer'>
    <slot />
</a>
